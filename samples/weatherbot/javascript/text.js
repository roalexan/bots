const GREETING = `Welcome to the weather bot!`;
const USAGE = `Enter the zipcode, and will give you a weather report`;
const REPLY = `Weather report: `;

module.exports = {
  GREETING, USAGE, REPLY
}
