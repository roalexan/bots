const GREETING = `Welcome to the feedback bot!`;
const USAGE = `Type anything, and I will echo it back to you and request feedback`;
const REPLY = `You said: `;

module.exports = {
  GREETING, USAGE, REPLY
}
